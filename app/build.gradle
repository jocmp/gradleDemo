def main = "com.tutorial.app.Main"

jar {
    manifest {
        attributes('Main-Class': main)
    }
}
 
dependencies {
      compile project(':common')
      compile project(':api')
      compile 'log4j:log4j:1.2.17'
}
